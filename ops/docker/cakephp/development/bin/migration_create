#! /bin/bash

if [[ $(docker ps -q -f name=hxhronie_cakephp) ]];
then
	read -p 'Table Name: ' table
	read -p 'Column:Type: ' columns
	docker exec -it hxhronie_cakephp /bin/bash -c "bin/cake bake migration Create${table} ${columns}"
else
	echo 'Container not running!'
fi