#! /bin/bash
containerName='hxhronie_cakephp'
if [[ $(docker ps -q -f name=$containerName) ]];
then
	read -p 'Table Name: ' table
	read -p 'Column:Type: ' columns
	docker exec -it $containerName /bin/bash -c "bin/cake bake migration Create${table} ${columns}"
else
	echo 'Container not running!'
fi